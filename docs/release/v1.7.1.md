## v1.7.1

### 修复

- **构建修复**：修复 `golangci-lint` 中 `gosec` 扫描器的误报问题
  - 为 `os.Stat` 文件存在性检查、`exec.Command` 可信路径调用等安全场景添加 `//nolint:gosec` 注释
  - 涉及文件：`cmd/msp/main.go`、`internal/handler/thumbnail.go`、`internal/media/probe.go`、`internal/media/transcoder.go`、`internal/media/transcoder_test.go`

- **符号链接处理**：`internal/media/transcoder.go` 使用 `os.Lstat` 替代 `os.Stat`，正确识别符号链接并拒绝处理，避免符号链接指向目录时绕过目录检查

### 工程

- **CI**：移除发布工作流中冗余的测试任务（`0cbb069`）
