## v1.7.2

### 修复

- **Dockerfile**：修复 Go 版本号错误（`1.25` → `1.24`），`docker build` 不再失败
- **Dockerfile**：固定 Alpine 基础镜像版本（`alpine:3.21`），确保构建可复现
- **Dockerfile**：移除 `CGO_ENABLED=0` 下无用的 `gcc`/`musl-dev` 安装
- **Dockerfile**：容器以非 root 用户运行（`appuser`），提升安全性
- **构建脚本**：修复 `scripts/build.sh` 中 4 处 bash `&&`/`||` 运算符优先级 bug，自定义构建目标现在能正确匹配 `amd64`/`x64` 别名

### 优化

- **日志系统**：修复 `RotateLogIfNeeded()` 与 `Log()` 之间的数据竞争，写入操作现在在锁内完成
- **日志系统**：缓存 `log.Logger` 实例，消除每次日志调用的堆分配开销
